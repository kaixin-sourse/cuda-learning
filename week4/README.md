# Week 4

## 主题

- sum reduction 的树形并行思路
- branch divergence
- coalesced memory access
- shared memory bank conflict 的基本认识

## 当前文件

- `week4_guide.md`：Week 4 详细讲义
- `CMakeLists.txt`：Week 4 工程配置
- `src/reduction_compare.cu`：三种 reduction 写法的正确性与计时对比

## 运行方式

在 CLion 中选择本目录下的 `CMakeLists.txt`，然后运行：

- `week4_reduction_compare`
