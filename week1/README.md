# Week 1

## 主题

- GPU 与 CPU 的基本区别
- CUDA 编程模型：thread、block、grid
- 第一个 CUDA 程序：vector add

## 当前文件

- `week1_guide.md`：Week 1 详细讲义
- `CMakeLists.txt`：Week 1 工程配置
- `src/vector_add.cu`：第一个可运行的 CUDA 示例

## 运行方式

在 CLion 中选择本目录下的 `CMakeLists.txt`，然后运行目标 `week1_vector_add`。
